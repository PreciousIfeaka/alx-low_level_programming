#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: first input character
 * @b: second input character
 * Return: Always mult (Success).
 */

int mul(int a, int b)
{
	int mult;

	mult = a * b;

	return (mult);
}
